# Helm values for app python-cost

appName: python-cost
namespace: python-cost-ns-dev

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 80

ingress:
  enabled: false
