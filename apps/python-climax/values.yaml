# Helm values for app python-climax

appName: python-climax
namespace: python-climax-ns-dev

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 80

ingress:
  enabled: false
